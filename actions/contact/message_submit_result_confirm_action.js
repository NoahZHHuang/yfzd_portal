import {createAction} from 'redux-actions'; 

import {
    MESSAGE_SUBMIT_RESULT_CONFIRM
} from './action_type';


export const MESSAGE_SUBMIT_RESULT_CONFIRM_ACTION = createAction(MESSAGE_SUBMIT_RESULT_CONFIRM);